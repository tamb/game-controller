import React from "react";
import ReactDOM from "react-dom";
import GameController from "./components/GameController";

const App = document.getElementById("app");

ReactDOM.render(<GameController />, App);
